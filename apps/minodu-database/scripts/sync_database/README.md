# Sync Database Script

Syncs the online backend with the local one

* example usage: `poetry run python sync.py http://minodu.chickenkiller.com:3000/v1/files/minodu_backend_backup.zip test --user=minodu_user --password=password`